name: vanestack_common
description: Shared models and utilities for the VaneStack Dart backend framework.
version: 0.1.0
repository: https://github.com/vanestack/vanestack
homepage: https://vanestack.dev
resolution: workspace
topics:
  - server
  - backend
  - serialization

environment:
  sdk: ^3.9.2

# Add regular dependencies here.
dependencies:
  path: ^1.9.0
  dart_mappable: ^4.6.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
  build_runner: ^2.9.0
  dart_mappable_builder: ^4.6.1
  